@host = http://localhost:3000

### Get all users
GET {{host}}/users

### Get single user with it's playlists
GET {{host}}/users/1

### Make new playlist for the user
POST {{host}}/users/1/playlists
Content-Type: application/json

{
  "name": "La mas perrona",
  "description": "playlist vareada mix mp3 extracool"
}